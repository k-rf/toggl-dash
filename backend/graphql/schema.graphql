type DashButton {
  id: ID!
  client: String!
  project: String!
  description: String!
  order: Int!
}

type Query {
  dashButtonAll: [DashButton!]!
}

# ------------------------------------------------------------------------------

input StartEntryInput {
  dashButtonId: String!
}

type Mutation {
  startEntry(data: StartEntryInput): Boolean
}
